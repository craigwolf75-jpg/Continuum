# CONTINUUM PROMPT 34a: The SIGMA Portal, and SIGMA on the Board Portal

**Ruling executed:** SIGMA gets its own standalone portal, stays active on the board portal, and gets its own tab on the hub. The first two shipped in this prompt as working artifacts; the hub tab is a site-repository change in the agent lane, and the exact mission text is included below for the Zeus queue.
**Date:** July 21, 2026

## 1. The SIGMA portal (sigma-portal.html, new)

The SIGMA administrator seat from the role map, now a working portal in the family chrome: a sidebar of six sections, its own storage key (continuum_sigma_portal_v1), demonstration data labeled throughout, and the honesty laws load-bearing on every screen. Overview shows the connection at a glance: workflow Proposed, method to be confirmed, live connection not yet established, with the four possible methods listed and a link to the connection page for the guided story. Case exchange holds the two queues: received from the system of record (the Jordan Miller demonstration case) and approved updates prepared for return, with the standing banner: prepared and held, nothing transfers until the method is confirmed and a person authorizes the send. Field mapping renders both directions field by field as the object technical discovery confirms. Sync history carries the timestamped events plus the honest integration-errors card (no exchange attempted; error handling built and ready), keeping the law that an empty result and a broken exchange must never look the same. Permissions states who sees what, the write path honestly pending, and the privacy wall as two columns: what travels through the exchange, and what never enters it, down to personal recovery journal entries. And Settings operationalizes yesterday's naming collision: a two-button choice between SIGMA-HR (the presentation specification) and SIGMA-RH (GardaWorld's own documentation, the RH being the French Ressources Humaines), with the rationale printed on the surface; the suite proves the choice flips the name across every screen, the brand line included, with the old name fully absent after the flip, and persists in the portal's own key. Per decision D13 the default is SIGMA-RH.

## 2. SIGMA on the board portal (wcb-portal.html, patched)

The board seat gains a SIGMA exchange section in its navigation, written entirely inside the seat's scope law: it explains where filings come from (the employer's system of record holds the incident and claim record, the CRTW layer prepares the filing, a person authorizes every send) and states the boundary in one sentence: this seat receives the submission itself, never the employer's internal record and never the worker's private recovery information. Below it, a submissions-by-channel rollup computed live from the seat's own claims using the plain channel names, links to both SIGMA artifacts (the connection page for the room, the SIGMA portal for the seat), and the demonstration disclaimer. The suite proves the doctrine line, the human-send law, the channel rollup, both links, the honest labels, and every standing regression: canon KPIs (19 open, 7 to acknowledge), the settings channels card, and the dashboard's plain-language milestone alert.

## 3. The hub tab (site repository, mission text for Zeus)

The hub role-select lives in the site repository under the agent group's custody, currently mid-redesign under the site stream's own Prompt 33, so the tab ships as a mission rather than a cross-repo patch from here. Paste into zeus-missions.md:

SITE-34a: Add a seventh card to the /hub role select: SIGMA Exchange. Same card component and interaction states as the six existing roles (blue at rest, gold on activation, per the Prompt 33 redesign), icon swap_horiz, subtitle The system-of-record connection, linking to the SIGMA portal artifact. The card copy follows the plain-language standard and never claims a live integration: the phrase is proposed workflow. Argus patrols the new card under the register scan; Heracles extends the hub suite to seven cards; the human gate does not apply (no consent, legal, pricing, or schema content).

## 4. Verification summary

SIGMA portal suite green: boot state, all six sections' content asserted, the proposed and not-yet-established labels, the prepared-and-held banner, the privacy columns, the name flip proven across screens with persistence, and isolation proven against all seven other storage keys. Board portal suite green including the new section and all regressions; the six-portal canon suite green; dash audit clean on both artifacts and this companion.

## Note (execution status, 2026-07-21)

Executed against main. deploy/sigma-portal.html (six sections, own key continuum_sigma_portal_v1, name toggle defaulting to SIGMA-RH per D13) and deploy/sigma-portal.test.mjs (38 assertions) are built and on main. deploy/wcb-portal.html gains the SIGMA exchange section and deploy/wcb-portal.test.mjs its eight new assertions (18 total). The SITE-34a mission is queued in zeus-missions.md for the agent lane; the hub tab itself is not built here. Full suite green, dash audit clean.
