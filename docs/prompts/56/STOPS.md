# Prompt 56 stop notes

Real stops only. Prompt 56 is REGISTERED, NOT RELEASED. This
prompt sits behind G1 and Prompt 53. Athena does not ship. No em
dashes or en dashes anywhere.

Do not execute worker product, persistence, identity provider,
Continuum Motion, conversational adapter, psych capture release,
screens, or live provisioning.

---

## Section 00.1. Registered, not released

Prompt 56 is registered. It is not released.

Section 00.1 binds. A later sequenced mission may release it.
These files do not.

---

## Prompt 53 holds

From `docs/prompts/53/HOLDS.md`. Still apply. Not relaxed here.

- HOLD Montreal project create. Do not create or modify a hosted
  Montreal project.
- HOLD Bedrock inference go. Do not send a Bedrock go. Do not
  enable inference.
- HOLD occupational or reference seeding beyond SYNTH-prefixed
  fixtures.
- HOLD Section 3 and onward live-platform work under Prompt 51
  foundations (foundations lineage, not the design-system
  folder).
- 50a Decision 1 remains SUSPENDED. Decision 2 stands. Do not
  create `mpi.person`. Do not expand the allow-list.
- Prompt 47 redo waits REV 2. Do not start the redo here.

---

## G1 gate

`PROMPT_G1_DISCOVERY_AUDIT` is not in hand. Do not start G1. Do
not invent G1.

`G1_AUDIT_REPORT.md` exists (2026-08-13, read-only discovery).
That file is not G1 closed. It does not release this prompt.

The wait remains G1 plus Craig sequence.

---

## PSYCH_CAPTURE_PRODUCTION_RELEASE is false

Source default is false
(`supabase/migrations/20260915140000_worker_schema.sql`).
`deploy/worker-schema-graph.test.mjs` asserts that default.

**STOP.** Do not flip the flag. Do not release psych capture.

---

## Generative adapter is disabled

`GENERATIVE_ADAPTER_ENABLED` defaults false.
`deploy/worker/app/app.js` hardcodes
`WK.companion.generativeEnabled = false` and throws if that field
is true.

**STOP.** Do not enable the conversational adapter. Do not invent
a provider.

---

## Continuum Motion is dark

`MOTION_PRODUCTION_RELEASE` defaults false.
`worker.record_movement` refuses writes while the flag is false.

**STOP.** Do not enable Continuum Motion. Do not add a Motion
screen.

---

## Document 13 is unbuilt

No Document 13 source or implementing file is in this
repository.

**STOP.** Do not invent Document 13. Do not build it here.

---

## Phase 7b is deleted

No Phase 7b file is in this repository.

**STOP.** Do not restore or rebuild Phase 7b.

---

## Amendment file not supplied

`PROMPT_54A_AMENDMENT_1.md` is not in this repository.
Applied-in-place is UNVERIFIED against the amendment file.

**STOP.** Do not invent Amendment 1 text.

---

## Version unconfirmed versus Craig 44 to 49 list

Craig's canonical versions list covered old stream 44 to 49
only. This document is Craig original PROMPT 54 / unified 56.

Version canonicality is UNCONFIRMED. CONFIRM WITH CRAIG before
any future build.

---

## Unified 54 Design System is a different prompt

`docs/prompts/54/` is the Design System and Surface Standard.
This folder is the worker experience.

**STOP.** Do not overwrite `docs/prompts/54/`. Cross-link only.

---

## No new identity provider

Existing hub and worker auth stay as they are. Do not add an
identity provider. Do not create `mpi.person`.

---

## No live schema apply. No live provisioning

`0018` and `0019` stay unapplied. Worker schema files stay files.
Do not apply them live. Do not provision a worker.

---

## package.json is locked

This registration does not require a `package.json` change. Do
not edit one.

---

## Athena does not ship

Merged and deployed. Registration docs only. Still not a product
release. G1 is not closed. Hermes is not a ship of worker
experience. Hermes ships only when Craig names ship.
