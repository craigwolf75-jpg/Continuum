# Prompt 64 stop notes

Real stops only. Prompt 64 is REGISTERED
in Continuum docs. Not a Continuum
product release. Draft PR. Registration
only. No em dashes or en dashes
anywhere.

Companions:
[REGISTER.md](REGISTER.md),
[SECTION_00.md](SECTION_00.md).

---

## Deploy gate

Draft PR. Registration only. No product
or runtime change. Hermes does not treat
this file as a ship order. Hermes ships
Continuum only when Craig names ship.

---

## No Continuum environment attach

Do not attach to a Continuum environment
without Craig named credentials via a
secret store. Never create a test
environment from chat. Never put
credentials in chat.

B1 waits on Craig and Gary. It intersects
the G1 audit environments census and the
platform decision. No test environment is
created while that wait stands.

**STOP.** No environment attach. No test
environment from chat. No credentials in
chat.

---

## Firecrawl needs Craig approval

B2. Firecrawl run authorization is an
external action. It needs Craig's
explicit approval.

**STOP.** Firecrawl waits on Craig.

---

## HIL-11 CIPS weight approval

B3. HIL-11 CIPS weight approval is
human. Four domain human review.
Allocation stays schema blocked until
that review completes.

**STOP.** HIL-11 allocation stays
blocked.

---

## STAT-Q1 / HIL-9 lost time versus total burden

B4. STAT-Q1 lost time versus total
burden is a human product decision
(HIL-9). Gary with Craig.

**STOP.** STAT-Q1 / HIL-9 waits on Gary
with Craig.

---

## No credentials in chat

When credentials move they move through
a secret store, never chat. Do not write
tokens, passwords, or secret values in
any file.

**STOP.** No credentials in chat.

---

## package.json locked

Do not edit the locked root
`package.json`. Do not edit any
`package.json`.

**STOP.** `package.json` stays locked.

---

## No live schema apply

Do not apply live schema. File only.
Draft PR only.

**STOP.** Do not apply live schema.

---

## Do not invent G1

`G1_AUDIT_REPORT.md` exists (2026-08-13,
read-only discovery). That file is not
G1 closed. `PROMPT_G1_DISCOVERY_AUDIT` is
not in this repository. Do not invent G1.
Do not start G1. Do not invent G1 closed.

Do not invent REV 2.

**STOP.** Do not invent G1. Do not invent
REV 2.

---

## Prompt 53 holds were released 2026-09-16 by Craig

From [../53/HOLDS.md](../53/HOLDS.md).
Former Prompt 53 holds are no longer
binding under Prompt 53. Hold lift is
not an auto-execute.

Named human dispatch still required
before Montreal, Bedrock, non-SYNTH seed,
or live schema apply.
