# Prompt 67 stop notes

Real stops only. Prompt 67 is
REGISTERED. Gate holds lifted by
Craig 2026-09-19. Draft PR. Not a
Continuum product release. Athena
does not ship. No em dashes or en
dashes anywhere.

Companions:
[REGISTER.md](REGISTER.md),
[SECTION_00.md](SECTION_00.md),
[ACCEPTANCE.md](ACCEPTANCE.md).

---

## CLOSED. Missing artifact
index.html

Checkout `deploy/index.html`
(48965 bytes at verification)
was verified this pass as the
Prompt 67 landing
(self-contained, PALETTE, ROUTES
object, pathway, no access-code
box, approved hero, Calendly,
`info@continuumrtw.com`) and is
used. After Athena comment-only
edits the working-tree file is
48749 bytes. Visitor copy
unchanged. Do not treat the trim
as a rewrite of the landing.
Athena USES THIS FILE. Do not
invent HTML. Do not rewrite
`deploy/index.html`.

Identity note, kept honest: the
file matches the Gary 20 August
2026 landing commit lineage
(`94e3a994` / later header tweak
`fd52d0b`). Craig named USE IT if
clearly Prompt 67. Do not claim
Continuum verified the other
session's eight-width Chromium
pass.

**CLOSED.** Checkout file
verified as Prompt 67 landing and
used. Do not invent HTML.

---

## CLOSED. Gate decision

Craig named both rulings
2026-09-19:

1. `/` and required static assets
   join the gate middleware allow
   list (publicly readable).
   Prefer allow-list, not ungating
   the whole site.
2. Access code experience moves
   behind Sign In. Reuse existing
   gate markup. Leave POST
   `/api/site-access` untouched.

Athena opens `/` and
`/index.html` on the allow list.
`/hub` and portals stay gated.
The holding page keeps the code
box.

**CLOSED.** Craig named
2026-09-19.

---

## CLOSED. Hero copy, Craig ICON

Craig named ICON approval
2026-09-19. Approved wording:
"Where medical decisions become
safe work" plus AI-powered
positioning. Founding line "Where
care ends, Continuum begins"
stays retired on the new landing.

**CLOSED.** Craig named
2026-09-19.

---

## CLOSED. Assessment path

`ROUTES.assessment` `/assessment/`
is verified in this repo
(`deploy/assessment/`). Live
outside-the-gate browser confirm
stays UNVERIFIED and does not
block this named BUILD.

**CLOSED.** Path verified in repo
as `/assessment/`.

---

## CLOSED. signIn /hub path

`ROUTES.signIn` `/hub` is verified
in this repo as
`deploy/hub/index.html`. Still
gated by design. Live
outside-the-gate browser confirm
stays UNVERIFIED and does not
block this named BUILD.

**CLOSED.** Path verified in
repo. Still gated by design.

---

## CLOSED as missing-file.
og-image.png

`deploy/og-image.png` exists
(34817 bytes, PNG 1200 by 630,
commit `622eb2eb`). The
missing-file STOP is closed.

Identity as the Prompt 40
founding-line asset stays
UNVERIFIED. That is a note, not a
missing-file STOP. Do not invent
a close of that identity
question.

**CLOSED** as a missing file.
Identity UNVERIFIED remains a
note.

---

## CAUTION, not a build STOP.
Calendly hand confirm

Calendly
`https://calendly.com/craig-continuumrtw`
is set in the landing and closes
booking-url-pending. Hand confirm
before go live may remain a
go-live caution (Teams trial
expired around 31 July; free plan
keeps one event type). It is not
a build STOP.

**CAUTION.** URL set. Hand
confirm is go-live caution, not a
build STOP.

---

## Ask list, not a build STOP.
Unseen docs and Nexus name

Unseen by this stream and on the
ask list: the approved landing
specification, and
`Continuum_MVP.html`.

Nexus is not a registered name in
this stream. Gary confirms what
"Nexus" names in the Calendly
distinction.

Do not invent them. These stay on
the ask list. They are not a
build STOP for this named BUILD.

---

## No live schema apply

Do not apply live schema. Do not
change schema. File only. Draft
PR only.

**STOP.** Do not apply live
schema.

---

## package.json locked

Do not edit the locked root
`package.json`. Do not edit any
`package.json`.

**STOP.** `package.json` stays
locked.

---

## Do not invent G1 or REV 2

Do not invent G1 closed. Do not
invent REV 2. Do not start G1. Do
not touch Section 5 HSE clinical
content.

G1 report exists and is not
closed. `docs/prompts/G1/G1_AUDIT_REPORT.md`
is on this tip (PR 182). That
file says it is not G1 closed.

**STOP.** Do not invent G1. Do
not invent REV 2.

---

## Unrelated Argus findings

Do not fix unrelated HYG-177-002,
HYG-175, or HYG-176 unless they
block this PR.

**STOP.** Do not fix unrelated
HYG-177-002 / HYG-175 / HYG-176
unless they block this PR.

---

## Do not claim Argus CLEAN

Do not claim Argus CLEAN in this
file. Argus will patrol
separately.

**STOP.** Do not claim Argus
CLEAN.

---

## Prompt 53 holds were released
2026-09-16 by Craig

From [../53/HOLDS.md](../53/HOLDS.md).
Hold lift is not an auto-execute.
Named human dispatch still
required before Montreal,
Bedrock, non-SYNTH seed, or live
schema apply.

**STOP.** Hold lift is not an
auto-execute.

---

## Deploy gate

Draft PR only. Athena does not
ship. Continuum ships only when
Craig names ship. Hermes ships
Continuum only when Craig names
ship. Do not treat this file as a
ship order. Do not invent a
Continuum product release.

**STOP.** Draft PR only. This
file is not a ship order. Athena
does not ship.
