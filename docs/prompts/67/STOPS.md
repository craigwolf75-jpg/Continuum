# Prompt 67 stop notes

Real stops only. Prompt 67 is
REGISTERED NOT RELEASED. Draft PR.
Registration plus honesty STOP. Not
a Continuum product release. No em
dashes or en dashes anywhere.

Companions:
[REGISTER.md](REGISTER.md),
[SECTION_00.md](SECTION_00.md),
[ACCEPTANCE.md](ACCEPTANCE.md).

---

## Missing artifact index.html

The complete 48 KB self contained
`index.html` exists only with Gary
and the prior session, as supplied.
It was not supplied to this stream
with this prompt.

Checkout `deploy/index.html` does
not close this. Identity of that
file as THE August 21 session
artifact is UNVERIFIED.

Do not invent HTML. Do not rewrite
`deploy/index.html` or the holding
page.

**STOP.** Artifact NOT IN HAND.
Checkout presence is not in-hand
for release.

---

## Gate decision (Gary)

Two rulings before any deployment:

1. Do `/` and its static assets join
   the gate middleware allow list,
   becoming publicly readable?
2. Does the access code experience
   move behind Sign In, reusing the
   existing markup, leaving
   `/api/site-access` untouched?

Deploying before both are ruled
would replace the gate and drop the
code box (regression). Completes
Prompt 65's open gate item when
ruled, per the note. Prompt 65 docs
in this checkout have no separately
titled "gate future at launch"
line.

Do not change gate middleware or
move the access code without Gary
ruling.

**STOP.** Both Gary gate rulings
are open. Middleware stays as is.

---

## Routes and Calendly before go live

`ROUTES.assessment` needs the real
path. The note: fallback scrolls
instead of 404.

`ROUTES.signIn` `/hub` is UNVERIFIED
from outside the gate.

Calendly
`calendly.com/craig-continuumrtw`:
confirm by hand before go live
(Teams trial expired around 31 July;
free plan keeps one event type).

Confirmed as is in the note:
`/privacy`, `/terms`, the mailto,
the in page anchors.

**STOP.** Assessment path, `/hub`
sign-in, and Calendly hand confirm
are open.

---

## Hero copy needs Craig ICON

The hero retires the founding line
"Where care ends, Continuum begins"
for "Where medical decisions become
safe work" plus AI powered
positioning. The note calls this the
approved wording. Confirm the
approval is Craig's (the ICON
lineage) before it ships.

**STOP.** Craig named ICON approval
is required before the hero copy
change. Athena does not ship that
line without it.

---

## og-image.png blocker carried

The note carries `og-image.png`
1200 by 630 missing as a BLOCKER
(referenced by meta tags, does not
exist).

Checkout now has
`deploy/og-image.png` (34817 bytes,
PNG 1200 by 630, commit
`622eb2eb5ed7741190424f73cf312512ad8b8344`).
Identity as the intended asset is
UNVERIFIED (Prompt 40 handoff said
locked logo plus the founding line).
Do not close the blocker.

**STOP.** og-image blocker stays
open. Checkout file does not close
it.

---

## Unseen docs and Nexus name

Unseen by this stream and on the
ask list: the approved landing
specification, and
`Continuum_MVP.html`.

Nexus is not a registered name in
this stream. Gary confirms what
"Nexus" names in the Calendly
distinction.

**STOP.** Unseen docs and Nexus
name stay on the ask list. Do not
invent them.

---

## No live schema apply

Do not apply live schema. Do not
change schema. File only. Draft PR
only.

**STOP.** Do not apply live schema.

---

## package.json locked

Do not edit the locked root
`package.json`. Do not edit any
`package.json`.

**STOP.** `package.json` stays
locked.

---

## Do not invent G1 or REV 2

Do not invent G1 closed. Do not
invent REV 2. Do not start G1.

**STOP.** Do not invent G1. Do not
invent REV 2.

---

## Unrelated Argus findings

Do not fix unrelated HYG-177-002,
HYG-175, or HYG-176 unless they
block this PR.

**STOP.** Do not fix unrelated
HYG-177-002 / HYG-175 / HYG-176
unless they block this PR.

---

## Do not claim Argus CLEAN

Do not claim Argus CLEAN in this
file. Argus will patrol separately.

**STOP.** Do not claim Argus CLEAN.

---

## Prompt 53 holds were released 2026-09-16 by Craig

From [../53/HOLDS.md](../53/HOLDS.md).
Hold lift is not an auto-execute.
Named human dispatch still required
before Montreal, Bedrock, non-SYNTH
seed, or live schema apply.

**STOP.** Hold lift is not an
auto-execute.

---

## Deploy gate

Draft PR only. Athena does not
ship. Continuum ships only when
Craig names ship. Hermes ships
Continuum only when Craig names
ship. Do not treat this file as a
ship order.

**STOP.** Draft PR only. This file
is not a ship order.
