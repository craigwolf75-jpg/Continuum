# Prompt 67. Section 00. Named BUILD
notes.

REGISTERED. Gate holds lifted by
Craig 2026-09-19. Draft PR. Not a
Continuum product release. Athena
does not ship.

Write date: 19 September 2026.
Registration date in the stream:
20 August 2026.
Named BUILD date: 2026-09-19.

Named BUILD notes. Craig named
BUILD Prompt 67 public landing onto
continuumrtw.com WITH GATE HOLDS
LIFTED. Still a draft PR. Still not
a ship order. Do not invent a
Continuum product release.

No visitor-facing product copy
beyond quoting the already-approved
hero line.

No em dashes or en dashes anywhere.

---

## Named rulings (Craig, dated
2026-09-19)

1. Public landing is the site root
   for continuumrtw.com.
2. Gate holds lifted: `/` and
   required static assets join the
   gate middleware allow list.
   Prefer allow-list, not ungating
   the whole site.
3. Access code experience moves
   behind Sign In. Reuse existing
   gate markup. Leave POST
   `/api/site-access` untouched.
4. ICON hero: "Where medical
   decisions become safe work"
   plus AI-powered positioning.
   Founding line "Where care ends,
   Continuum begins" stays retired
   on the new landing.
5. Calendly
   `https://calendly.com/craig-continuumrtw`
   closes booking-url-pending.
6. Footer and contact:
   `info@continuumrtw.com`
   (Prompt 62a).

Athena opens `/` and `/index.html`
on the allow list. `/hub` and
portals stay gated. The holding
page keeps the code box. POST
`/api/site-access` stays
untouched.

---

## Artifact used after verification

Checkout `deploy/index.html`
(48965 bytes at verification) is
the Prompt 67 landing used for
this named BUILD. After Athena
comment-only edits (ROUTES
comment plus og-image comment)
the working-tree file is 48749
bytes. Visitor copy unchanged.
Do not treat the trim as a
rewrite of the landing.

Verified in this checkout, this
pass:

- self-contained
- PALETTE
- ROUTES object
- pathway
- no access-code box
- approved hero
- Calendly as named
- `info@continuumrtw.com`

Athena USES THIS FILE as the
public root after allow-list work.
Do not invent or reconstruct HTML.
Do not tell Athena to rewrite the
landing.

Identity note, kept honest: the
file matches the Gary 20 August
2026 landing commit lineage
(`94e3a994` / later header tweak
`fd52d0b`). Craig named USE IT if
clearly Prompt 67. This mission
treats it as the landing in hand
for the build.

Do not claim Continuum verified
the other session's eight-width
Chromium pass.

The other session's live-site
confirmations (every path gated to
unauthenticated visitors, POST
`/api/site-access` and
`/api/marketing-lead` live, token
stylesheet unread from outside the
gate, Vercel project
`continuum-o51l` API refusals)
remain that session's claims.
Continuum has not verified that
session's browser pass.

---

## Routes (repo verified, live
outside-the-gate UNVERIFIED)

Repo facts verified this pass.
A live outside-the-gate browser
confirm is still missing. That
label is UNVERIFIED. It does not
block this named BUILD.

- `ROUTES.assessment` `/assessment/`
  verified in this repo
  (`deploy/assessment/`, including
  `deploy/assessment/index.html`).
- `ROUTES.signIn` `/hub` verified
  as `deploy/hub/index.html`.
  Still gated by design.
- `ROUTES.contact` Calendly
  `https://calendly.com/craig-continuumrtw`
  as named. URL is set in the
  landing.
- Privacy `/privacy` and terms
  `/terms` verified as
  `deploy/privacy.html` and
  `deploy/terms.html`. Those paths
  are already on the current allow
  list.
- In-page anchors remain in-page
  (`#pathway`, and the other
  ROUTES anchors).
- Mailto
  `info@continuumrtw.com`
  (Prompt 62a) is in the landing
  footer and contact line.

---

## Hero copy, named

Craig named ICON approval
2026-09-19. The approved line is
"Where medical decisions become
safe work" plus AI-powered
positioning. The founding line
"Where care ends, Continuum
begins" stays retired on the new
landing.

---

## Calendly and booking-url-pending

The Calendly URL in the landing
closes the booking-url-pending
placeholder recorded since Prompt
62. Hand confirm before go live
may remain a go-live caution. It
is not a build STOP.

---

## Conformance, other session

Endorsed by the other session.
Continuum UNVERIFIED. Continuum
has not verified that session's
browser pass. Do not mark WCAG or
browser-pass PASS.

- No statistic, dollar figure,
  testimonial, or claim needing a
  source anywhere.
- No mechanism exposure (no
  scoring, matching, WCB mapping,
  or data model on the page).
- Fictional product data visibly
  captioned as fictional.
- WCAG AA measured with zero
  failures.
- The page renders fully with
  JavaScript disabled.
- No browser storage.
- One external request.

These remain Continuum UNVERIFIED
unless re-verified in this stream.
This pass did not re-verify them.

---

## Names and documents still unseen

Unseen by this stream and still
on the ask list: the approved
landing specification and
`Continuum_MVP.html`. Do not
invent them.

The note distinguishes the
Continuum Calendly from "the
Nexus one". Nexus is not a
registered name in this stream.
Gary confirms what it is. Do not
invent it.

---

## og-image

`deploy/og-image.png` exists
(34817 bytes, 1200 by 630, commit
`622eb2eb`). The missing-file
blocker is closed as a missing
file.

Identity as the Prompt 40
founding-line asset stays
UNVERIFIED. Do not invent a close
of that identity question.

Athena replaced the stale
"needs to be added" comment in
the landing HTML with a
comment-only note: the file
exists at `/og-image.png` (1200
by 630); missing-file blocker
closed; Prompt 40 identity
UNVERIFIED. Visitor copy
unchanged. That HTML comment is
no longer stale.

---

## Checkout observation (this tip)

Observations on tip
`326cc065c9d8657a3f4695c9c958c59c5f80398a`
(PR 182). Write date 19 September
2026. Named BUILD date
2026-09-19.

- `docs/prompts/67/` exists from
  the registration pass. This
  pass updates those four files.
- `deploy/index.html` exists.
  48965 bytes at verification.
  48749 bytes after Athena
  comment-only trim (ROUTES
  comment plus og-image
  comment). Visitor copy
  unchanged. Do not treat the
  trim as a rewrite. Git lineage:
  `94e3a994` (20 August 2026),
  author Gary Ferenczi, "Add
  public landing page (Prompt
  67), as built, on a branch".
  Later header tweak `fd52d0b`.
  Used after verification as the
  Prompt 67 landing.
- Gate history: `0cd7c77` opened
  `/` for Prompt 67. `f9cf2b3`
  (2 September 2026, Gary)
  re-gated `/` and `/index.html`
  behind the holding page with
  the code box. On committed tip
  `326cc065`,
  `deploy/middleware.js` still
  gates `/` and `/index.html`.
  Those two paths are not on
  `ALWAYS_PUBLIC_EXACT` in that
  commit. Athena opens `/` and
  `/index.html` on the allow list
  under the named rulings.
  Prefer allow-list, not ungating
  the whole site. Athena's
  working-tree allow-list now
  includes `/` and `/index.html`.
  That working-tree change is
  not shipped. This voice pass
  did not edit middleware.
- `deploy/og-image.png` exists
  (34817 bytes, PNG 1200 by 630,
  commit `622eb2eb`).
  Missing-file blocker closed.
  Identity UNVERIFIED.
- Holding page
  `deploy/gate/holding.html`
  still has the access-code box
  and posts to
  `/api/site-access`. That stays.
  Sign In `/hub` is the gated
  path to that experience.
- Prompt 65 docs in this checkout
  verify the live holding-page
  gate. They do not contain a
  separately titled "gate future
  at launch" line. Do not invent
  a Prompt 65 quote that is not
  in those files.

Do not paste or reconstruct
landing-page HTML. Paths, SHAs,
and byte sizes only.

---

## Deployment, still not a ship
order

Named BUILD work: rule the allow
list for `/` and `/index.html`,
reuse the checkout landing as the
site root, keep `/hub` and
portals gated, keep the code box
on the holding page, leave
`/api/site-access` untouched.

Athena does not ship. Hermes
ships Continuum only when Craig
names ship. These files are not a
ship order. Do not invent a
Continuum product release.

---

## Cross-links

- [REGISTER.md](REGISTER.md)
- [STOPS.md](STOPS.md)
- [ACCEPTANCE.md](ACCEPTANCE.md)
- [../62/REGISTER.md](../62/REGISTER.md)
- [../62a/REGISTER.md](../62a/REGISTER.md)
- [../65/REGISTER.md](../65/REGISTER.md)
- [../65/SECTION_1.md](../65/SECTION_1.md)
