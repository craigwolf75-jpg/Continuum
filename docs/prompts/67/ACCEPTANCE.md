# Prompt 67 acceptance

REGISTERED. Gate holds lifted by
Craig 2026-09-19. Draft PR. Not a
Continuum product release. Athena
does not ship.

Write date: 19 September 2026.
Registration date in the stream:
20 August 2026.
Named BUILD date: 2026-09-19.

Branch
`cursor/prompt-67-public-landing-6065`,
from base tip
`326cc065c9d8657a3f4695c9c958c59c5f80398a`
(PR 182: docs, Prompt G1
discovery audit report, URGENT
Section 5, not closed).

This file records acceptance
progress. It is not ship-ready.
It is not a product release.
Prompt 53 holds were released
2026-09-16 by Craig. Hold lift is
not an auto-execute.

Do not claim Argus CLEAN. Do not
claim ship-ready. Do not invent
G1 closed or REV 2. Do not mark
other-session browser conformance
PASS.

Calliope owns [REGISTER.md](REGISTER.md),
[SECTION_00.md](SECTION_00.md), and
[STOPS.md](STOPS.md). This file
does not overwrite those.

No em dashes or en dashes
anywhere.

---

## Progress on (a), (b), and (c)

The three items that blocked the
registration pass have progressed
for this named BUILD. Progress is
not ship-ready.

- (a) Artifact used after
  verification. Checkout
  `deploy/index.html` (48965
  bytes) is the Prompt 67 landing
  (self-contained, PALETTE,
  ROUTES object, pathway, no
  access-code box, approved hero,
  Calendly, `info@continuumrtw.com`)
  and is used. Athena USES THIS
  FILE. Do not invent HTML.
  Identity note: matches the Gary
  20 August 2026 landing commit
  lineage (`94e3a994` / later
  header tweak `fd52d0b`). Craig
  named USE IT if clearly Prompt
  67. Do not claim Continuum
  verified the other session's
  eight-width Chromium pass.
- (b) Both gate rulings named by
  Craig 2026-09-19. (1) `/` and
  required static assets join the
  allow list. Prefer allow-list,
  not ungating the whole site.
  (2) Access code moves behind
  Sign In, reuse markup, leave
  `/api/site-access` untouched.
- (c) Craig named ICON approval
  2026-09-19 for the hero ("Where
  medical decisions become safe
  work" plus AI-powered
  positioning). Founding line
  "Where care ends, Continuum
  begins" stays retired on the
  new landing.

Progress on (a), (b), and (c) is
not a ship-ready claim. Do not
claim Argus CLEAN. Do not mark
other-session browser conformance
PASS.

---

## What this draft does

Still draft PR only.

- Allow-list `/` and
  `/index.html` (Athena's named
  work). `/hub` and portals stay
  gated.
- Access code remains on
  `deploy/gate/holding.html`
  behind Sign In `/hub`.
- POST `/api/site-access`
  untouched.
- Landing file reused. Do not
  invent or reconstruct HTML. Do
  not tell Athena to rewrite it.
- These four Calliope docs
  updated.

Athena does not ship. Hermes
ships Continuum only when Craig
names ship. Do not invent a
Continuum product release.

---

## Conformance claims from the
note

The other session endorsed these.
Continuum UNVERIFIED. Continuum
has not verified that session's
browser pass. This pass did not
re-verify them. Do not mark any
of these PASS.

- No statistic, dollar figure,
  testimonial, or claim needing a
  source.
  UNVERIFIED (other session).
- No mechanism exposure (no
  scoring, matching, WCB mapping,
  or data model on the page).
  UNVERIFIED (other session).
- Fictional product data visibly
  captioned as fictional.
  UNVERIFIED (other session).
- WCAG AA measured with zero
  failures.
  UNVERIFIED (other session).
- Page renders fully with
  JavaScript disabled.
  UNVERIFIED (other session).
- No browser storage.
  UNVERIFIED (other session).
- One external request.
  UNVERIFIED (other session).
- Horizontal scroll none at 1440,
  1366, 1024, 768, 430, 390, 375,
  320.
  UNVERIFIED (other session).
- Keyboard tab order as claimed
  in the note.
  UNVERIFIED (other session).
- Focus ring as claimed in the
  note.
  UNVERIFIED (other session).
- Skip link as claimed in the
  note.
  UNVERIFIED (other session).
- Heading outline as claimed in
  the note.
  UNVERIFIED (other session).
- prefers-reduced-motion as
  claimed in the note.
  UNVERIFIED (other session).
- Mobile menu as claimed in the
  note.
  UNVERIFIED (other session).
- Assessment fallback as claimed
  in the note.
  UNVERIFIED (other session).
- Smooth scroll as claimed in
  the note.
  UNVERIFIED (other session).

Do not claim Continuum verified
the other session's browser pass.
Do not mark WCAG or browser-pass
PASS.

---

## Open items that are not
ship-ready claims

These are not acceptance passes.
They stay with [STOPS.md](STOPS.md)
as notes, cautions, or standing
stops.

- Live outside-the-gate browser
  confirm of `/assessment/`,
  `/hub`, `/privacy`, and
  `/terms` stays UNVERIFIED. Repo
  paths are verified. The label
  does not block this named
  BUILD.
- Calendly
  `https://calendly.com/craig-continuumrtw`
  is set in the landing. Hand
  confirm before go live is a
  go-live caution, not a build
  STOP.
- `og-image.png` missing-file
  blocker is closed (34817 bytes,
  1200 by 630, commit
  `622eb2eb`). Identity as the
  Prompt 40 founding-line asset
  stays UNVERIFIED.
- Unseen docs: approved landing
  specification,
  `Continuum_MVP.html`. Do not
  invent them.
- Gary confirms what "Nexus"
  names. Do not invent it.

---

## Independent STOPs still
standing

Prompt 53 holds were released
2026-09-16 by Craig. Former
Prompt 53 holds are no longer
binding under Prompt 53. Hold
lift is not an auto-execute.

- Draft PR only. Athena does not
  ship. Hermes ships Continuum
  only when Craig names ship.
- Named human dispatch still
  required before Montreal,
  Bedrock, non-SYNTH seed, or
  live schema apply.
- No live schema apply.
- Locked root `package.json`
  stays locked.
- Do not invent G1 closed or
  REV 2. G1 report exists and is
  not closed. Do not start G1.
  Do not touch Section 5 HSE
  clinical content.
- Do not claim Argus CLEAN.
- Do not fix unrelated
  HYG-177-002, HYG-175, or
  HYG-176 unless they block this
  PR.
- Do not invent, reconstruct, or
  fabricate landing-page HTML.
- Do not tell Athena to rewrite
  `deploy/index.html`.
- Do not ungate the whole site.
- Do not change POST
  `/api/site-access`.

---

## What this draft did not do

- Did not invent or reconstruct
  landing-page HTML.
- Did not edit
  `deploy/index.html`,
  `deploy/gate/holding.html`, or
  `deploy/middleware.js`.
- Did not apply a migration.
- Did not edit any
  `package.json`.
- Did not invent G1 closed or
  REV 2.
- Did not claim Argus CLEAN.
- Did not close og-image
  identity. Closed only the
  missing-file blocker.
- Did not mark conformance PASS.
- Did not claim Continuum
  verified the other session's
  eight-width Chromium pass.
- Did not claim ship-ready.
- Did not ship.
- Did not invent a Continuum
  product release.
