# Prompt 68. Section 00. Registration
and execution discipline.

REGISTERED NOT RELEASED. Draft PR.
Registration only. Not a Continuum
product release. Not a clinician
product PASS.

Write date: 19 September 2026.
Registration date in the stream:
21 August 2026.

Registration notes only. Identity,
status, scoped supersession, citation
map, artifact manifest, version flag,
and standard clauses. Taken from
Section 00 of the 21 August 2026
package. The package's own rules
below stand unaltered.

Companion HTML files were not
supplied to this stream. Do not
invent them here.

No em dashes or en dashes anywhere.

---

## 00.1 Status: REGISTERED, NOT RELEASED

The authenticated clinician product
is the platform lane.

This package sits behind:

1. the G1 discovery audit
2. Craig's platform decision
3. the Prompt 53 hold family, like
   unified 56 and 58 before it

That includes its own removal
instruction: the bulk accelerator
withdrawal and any row migration
execute only when the lane is
released.

Its final section's ship gate stands
on top: nothing ships before the hub
authentication fix is deployed and
verified by Craig.

Craig later released Prompt 53
programme holds on 2026-09-16. See
[../53/RELEASE.md](../53/RELEASE.md).
Hold lift is not an auto-execute. Do
not invent a Prompt 68 release.

Hub auth remains UNVERIFIED. Prompt
52 Check 1 found no repository record
that Craig verified Prompt 33. Unit
suites are not a Craig verification.
See [../52/SECTION_1.md](../52/SECTION_1.md).

`G1_AUDIT_REPORT.md` exists at repo
root (2026-08-13, read-only
discovery). That file is not G1
closed. `PROMPT_G1_DISCOVERY_AUDIT`
is not in this repository. Do not
invent G1. Do not start G1.

---

## 00.2 Supersession, scoped exactly
as the package scopes it

This package supersedes every earlier
clinician SCREEN DESCRIPTION, and
wins on conflict for the clinician
experience only.

The safety laws of unified Prompts 39
to 46 persist except where this
package names a supersession.

One is named: the two zone model is
superseded and the bulk accelerator
is withdrawn package wide, which
strengthens the posture unified 41's
never-defaults rule guarded.

Any other apparent conflict between
this package and a 39 to 46 law is a
stop and report. Silence never
repeals a law.

The carried-forward laws visibly
intact here:

- `ai_draft` blocks signature
- the draft label is real page text,
  never a pseudo element
- the UNCHANGED affirmation is a
  clinical statement, not a default
- the 90 day stale rule
- jurisdiction resolution that blocks
  with a named message rather than
  falling back

Do not apply the bulk accelerator
withdrawal or any row migration in
this registration. Execute only when
the lane is released.

---

## 00.3 Citation map

Craig's numbering, translated in
place.

| Craig wrote | Unified | What it is |
|---|---|---|
| Prompt 38 | 41 | Physician Interface (four citations) |
| Prompt 51 | 58 | Design System and Surface Standard (one citation) |
| Prompt 42 | 45 | Jurisdiction Layer (one citation) |

The Developer Build Package remains
the named hard dependency it was
under unified 41. Do not invent it.

---

## 00.4 Artifact manifest

Build to these and not to a
reinterpretation of them.

| File | Expected md5 | Status this pass |
|---|---|---|
| `clinician-home.html` | `64ff54939f597c8cee3cecb5f730d29d` | NOT IN HAND |
| `first-report.html` | `bb4a14c8c036d2990777612f02828511` | NOT IN HAND |

The package says both ship with this
registration under the exact
filenames the companion table uses.
Both verified dash free, zero
external requests, draft labels as
real DOM text.

They were NOT supplied in this
upload. A workspace walk for those
filenames finds zero files. Do not
invent them. Do not reconstruct them
from `deploy/measurement.html`,
`deploy/followup.html`, or
[../52/screens/PHYSICIAN.md](../52/screens/PHYSICIAN.md).

Ask list: upload the originals, or
Craig confirms they will not be
supplied.

---

## 00.5 Version flag

This package postdates Craig's
canonical versions list, the same
flag family as unified 54, 55, 56,
and 58. His confirmation clears it.

Until that confirmation, version
canonicality is UNCONFIRMED.

---

## 00.6 Standard clauses

- Credentials never in chat.
- Python dash audits on everything
  written.
- Evidence or UNVERIFIED on every
  prerequisite.
- SYNTH prefixed test data only
  while the hold stands.
- Append only migrations when the
  lane releases.

This registration writes docs only.
It does not migrate. It does not
seed. It does not ship.

---

## Locked terminology (from the
Developer Build Package section 0.2)

Use these in code, database,
interface, and conversation.

Banned in every label, tooltip, empty
state, error, and placeholder:
predicted, suggested diagnosis,
recommended restriction, smart,
automatic assessment, AI decided.

Use instead: proposed for your
review, drafted, not yet reviewed,
carried forward from 12 Aug, worker
reported.

Never expose to a practitioner: data
models, form engine internals, rule
engine internals, the occupational
database, algorithms, model
architecture, prompt systems, or
board mapping mechanics. These are
implementation details.

---

## Ship prerequisite, before any of it

Is the hub authentication fix
deployed and verified by Craig?
Nothing ships before it is. Report
status and stop if not. Source:
Prompt 41 section 1, restated in this
package's BUILD ORDER.

This registration reports: UNVERIFIED.
Stop.

---

## Outstanding Craig decisions
(package section M)

Four. Logged here. Not resolved here.

1. OQ-008. Who completes the return
   to work section in a real clinic,
   the practitioner or the office
   assistant? Build for the
   practitioner. Architect so the
   answer can change without a
   rebuild. Until Craig answers, no
   non practitioner role may write a
   capability value.
2. OQ-016. Do we hold ambient
   recordings at all, and for how
   long? Card A wording currently
   says recordings are deleted after
   30 days. That sentence is a
   promise to a patient. It must
   match reality before anyone reads
   it. Consent language is a human
   gate. Do not ship that text.
3. Nurse practitioners. The board's
   role code list holds nine codes
   and does not include nurse
   practitioner, while its contract
   mapping table does reference the
   role. Until the board resolves it,
   NP support cannot be claimed and
   must be blocked at configuration.
   Whether to write the board is
   Craig's.
4. Saskatchewan and Ontario. Neither
   has a form pack, so neither is
   operable. Architecture only. A
   silent fallback sends a
   Saskatchewan report on an Alberta
   form under a real practitioner's
   real credential. Do not seed
   speculative form packs.

---

## What varies by jurisdiction, and
nothing else

Form definitions and code lists. The
fee schedule. Which consents are
required. What may lawfully reach an
employer. Statutory deadlines and
cutoffs. The statutory holiday table.
The submission channel. The
practitioner credential label and its
format.

No component contains a jurisdiction
conditional. Alberta is the only
operable form pack named in the
package. Saskatchewan and Ontario are
architecture only.

---

## Cross-links

- [REGISTER.md](REGISTER.md)
- [STOPS.md](STOPS.md)
- [ACCEPTANCE.md](ACCEPTANCE.md)
- [PACKAGE.md](PACKAGE.md)
- [../53/HOLDS.md](../53/HOLDS.md)
- [../53/RELEASE.md](../53/RELEASE.md)
- [../52/SECTION_1.md](../52/SECTION_1.md)
- [../56/REGISTER.md](../56/REGISTER.md)
- [../58/REGISTER.md](../58/REGISTER.md)
