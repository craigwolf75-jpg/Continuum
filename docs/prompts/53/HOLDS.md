# Prompt 53 holds

Binding list for agents. Effective now. One page. A hold is an order,
not a suggestion.

No em dashes or en dashes anywhere.

---

HOLD Montreal project create. Do not create or modify a hosted Montreal
project.

HOLD Bedrock inference go. Do not send a Bedrock go. Do not enable
inference.

HOLD occupational or reference seeding beyond SYNTH-prefixed fixtures.
Do not seed anything else.

HOLD Section 3 and onward live-platform work under Prompt 51
foundations. This hold is the foundations lineage, not the
design-system folder.

SUSPENDED: 50a Decision 1 (Supabase as-is GO). Do not treat it as an
active GO. It survives as analysis only. The platform decision is
Craig's, after the G1 report.

STANDS: 50a Decision 2. Platform schemas versus hub, site, demo, and
worker tables. Exclusion by schema. One-entry `mpi.person` allow-list.
Do not create `mpi.person`. Do not expand the allow-list.

FREEZE local and CI work already complete. Do not unwind it. The hold
arrived in time. Nothing live to unwind.

G1 waits for paste. `PROMPT_G1_DISCOVERY_AUDIT` is not in hand. Do not
start G1. Do not invent G1.

Prompt 47 redo waits for Craig's REV 2 attachment. Craig confirmed the
clinic and enterprise file used for shipped Prompt 47 was not REV 2
from 12:32 PM. Result: UNVERIFIED failed / WRONG FILE / REDO REQUIRED.
Do not start the redo.

Old Prompts 44 to 46 are reading, not building. Old Prompts 47 to 49
halt on their own prerequisite checks by design. That halt is intended
behaviour, not failure.

More prompts exist that have not been sent. The current set is not the
complete programme. Do not treat it as complete.

Master Architecture is number 48. On arrival it must be RREV 2 sent
12:34 PM. Do not invent it.

Athena does not ship. No live apply. No merge as a ship. Draft
registration only.
